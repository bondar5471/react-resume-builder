import { makeStyles } from '@material-ui/core/styles';

export const useStyles = makeStyles(() => ({
  head: {
    padding: '10px',
  },
  spinner: {
    padding: '40px',
    marginLeft: '44%',
  },
}));
