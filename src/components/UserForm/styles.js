import { makeStyles } from '@material-ui/core/styles';

export const useStyles = makeStyles(() => ({
  uploadButton: {
    marginTop: 12,
    textTransform: 'none',
  },
  avatar: {
    width: '15%',
    display: 'block',
    marginLeft: 'auto',
    marginRight: 'auto',
  },
  photoButton: {
    marginTop: '10px',
  },
}));
