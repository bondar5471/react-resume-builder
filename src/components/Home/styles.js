import { makeStyles } from "@material-ui/core/styles"

export const useStyles = makeStyles(() => ({
  appBar: {
    flexGrow: 1,
  },
  // brightnessButton: {
  //   marginLeft: "85%",
  // },
  logo: {
    maxWidth: "100%",
    height: "auto",
  },
}))
