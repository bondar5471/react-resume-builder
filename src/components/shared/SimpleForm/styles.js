import { makeStyles } from '@material-ui/core/styles';

export const useStyles = makeStyles(() => ({
  section: {
    marginTop: '15px',
    padding: '16px',
  },
  marginBottom: {
    marginBottom: '25px',
  },
}));
